/*

Exercício 1 - Refaça o código implementado na aula antrior seguindo as seguintes diretrizes:

// Calculate the determinant of matrix M
void GetDeterminant(M, det)
{
    // Calculate determinant
}

// Fill the adjugate matrix of M
void GetAdj(M, adj)
{
    // Fill adj
}

// Multiply matrix M by vector b, result stored in sol
void Multiply(M, b, sol)
{
    // sol = M . b
}

// Calculate the inverse of matrix M and store in inv
void GetInv(M, inv)
{
    float det;
    float adj;

    // Calculate determinant and adjugate
    GetDeterminant(M, det);
    GetAdj(M, adj);

    // Fill the inverse matrix inv
}

// Solve the linear system Mx = b and store the result in sol
void GetSolution(M, b, sol)
{
    float inv;

    // Calculate the inverse of matrix M
    GetInv(M, inv);

    // Multiply the inverse matrix by vector b to get the solution
    Multiply(inv, b, sol);
}

*/
