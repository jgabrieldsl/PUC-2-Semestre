DROP TABLE ACCOUNTS;
DROP SEQUENCE SEQ_ACCOUNTS;

CREATE TABLE ACCOUNTS (
    ID INTEGER NOT NULL PRIMARY KEY,
    COMPLETE_NAME VARCHAR2(500) NOT NULL,
    EMAIL VARCHAR2(500) NOT NULL UNIQUE,
    PASSWORD VARCHAR2(64) NOT NULL,
    CREATED_AT DATE DEFAULT SYSDATE NOT NULL,
    CREATE SEQUENCE SEQ_ACCOUNTS START WITH 1 INCREMENT BY 1 /* Incremento no OracleDB */
);

INSERT INTO ACCOUNTS (ID, COMPLETE_NAME, EMAIL, PASSWORD)
VALUES (SEQ_ACCOUNTS.NEXTVAL, 'João Gabriel', 'joao.gsl@puccampinas.edu.br', '123');

INSERT INTO ACCOUNTS (ID, COMPLETE_NAME, EMAIL, PASSWORD)
VALUES (SEQ_ACCOUNTS.NEXTVAL, 'Felipe', 'felipe.nl1@puccampinas.edu.br', '123');

COMMIT;